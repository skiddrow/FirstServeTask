12 + 45 * 2 ^ 10 -4 * y
с помощью регулярок для всех типов
[Const, Plus, Const, Mult, Const, Power, Const, Minus, Const, Mult, Parameter]


input -> obj model(by parser) -> exp tree

Вопросы на 30.11
- как все таки правильно распарсить строку с помощью регулярок?
- как написать BuildFrom<Func<double, double, double>>(formula);?
- в интерпретаторе какую модель контекста выбрать?
- как при проходе коллекции с экспрешенами удалять элементы и при этом полностью пройти коллекцию корректно

СДЕЛАТЬ
_______________
2) парсер не занимается упаковкой Expression'ов, этим занимается отделтьный класс(паттерн Visitor)
3) подумать над тем, когда выражение начинается со знака -
4) BuildFrom<Func<double, double, double>>(formula) => BuildFrom<T>(formula) => T = Func<double, double, double>

СДЕЛАНО
_______________
+) парсим с помощью регулярок(каждая регулярка проверяется по началу строки)
+) проверка на то, что в начале стоит число/переменная, а не знак и наличе подряд двух арифметических операторов


  0  1 2
[,+,,-,3]
for
1 - [1] = ex(0,2);
2 -
3 -
4 -

[ex + ex * ex + ex]
[ex + ex + ex]

Expression.Lambda